# Source-only placeholder. A native package must not ship the proprietary DMG,
# generated application, or historical Rust/WebKit public-web client.
pkgname=chatgpt-work-linux
pkgver=@PKGVER@
pkgrel=1
pkgdesc='Source tooling for a local unofficial ChatGPT.dmg Linux compatibility build'
arch=('any')
license=('MIT')
source=()
sha256sums=()

prepare() {
  error 'Binary packaging is disabled: build the reviewed ChatGPT.dmg locally with make update-user.'
  return 1
}

package() {
  return 1
}
